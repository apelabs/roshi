import React from 'react';
import RoshiBaseModal from './RoshiBaseModal';

const RoshiWarningModal = ({ message }) => <RoshiBaseModal variant="warning" message={message} />;

export default RoshiWarningModal;
